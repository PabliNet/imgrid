<!-- "Compartir" desde la Galería u otra app: el usuario toca el ícono
     compartir sobre una imagen y elige Imgridroid de la lista. -->
<intent-filter>
    <action android:name="android.intent.action.SEND" />
    <category android:name="android.intent.category.DEFAULT" />
    <data android:mimeType="image/*" />
</intent-filter>

<!-- "Abrir con": el usuario toca una imagen en un administrador de
     archivos (o similar) y elige abrirla con Imgridroid directamente. -->
<intent-filter>
    <action android:name="android.intent.action.VIEW" />
    <category android:name="android.intent.category.DEFAULT" />
    <category android:name="android.intent.category.BROWSABLE" />
    <data android:mimeType="image/*" />
</intent-filter>
